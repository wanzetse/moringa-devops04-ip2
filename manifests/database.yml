apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ip4_wan2_stlfset
spec:
  selector:
    matchLabels:
      app: myapp
  serviceName: mongo_service
  replicas: 1
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: mongo
        ports:
        - containerPort: 27017
          name: db
        volumeMounts:
        - name: dbvol
          mountPath: /data/db
  volumeClaimTemplates:
  - metadata:
      name: dbvol
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
